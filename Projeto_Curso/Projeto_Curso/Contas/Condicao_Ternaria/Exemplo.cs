using System;
using System.Collections.Generic;
using System.Globalization;
using System.Text;

namespace Projeto_Curso.Contas.Condicao_Ternaria
{
    class Exemplo
    {
        /// <summary>
        /// Estrutura opcional do if else quando se deseja decidir um VALOR com base em uma condição.
        /// </summary>
        ///// <param name="args"></param>
        //static void Main (string [] args)
        //{
            
        //    double preco = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);
        //    ///                 condição   ?  valor vdd  ?  valor falso
        //    /*double desconto = (preco < 20) ? preco * 0.1 : preco * 0.05; */// caso preço for menor que vinte ele tera desconto de 1% se não tera desconto de 5%.

        //    Console.WriteLine(desconto);
        //}
    }
}
